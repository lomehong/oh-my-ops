id: 'OPSP-P22'
title: 'compose 探测缺陷 8：v1 用「执行探测」而非「存在性探测」，二进制缺失时异常穿透、绕过「两者皆无」建议分支'
state: '已落定'
level: 'L1'
created: '2026-09-16T09:07:28.784Z'
accept:
  - '探测统一包 try/catch（spawn 失败视作不可用）；v1 改用 `command -v docker-compose` 存在性探测（不执行本体）'
  - '「两者皆无」分支可达并输出平台分支建议（≥19.03 建议 v2 插件；<19.03 明说无插件机制、建议 v1 单文件）'
  - 'docker --version 解析健壮（18.09.6 / 24.0.7 等版式；v 前缀可选）'
  - '守卫：v1 spawn 抛错仍落到建议分支（新例）；全缺/v1 回退/v2 可用三例保持；npm run test: 'ci 全绿'''
events:
  - ts: '2026-09-16T09:07:28.784Z'
    op: 'create'
    by: 'omp-architect'
    note: 'compose 探测缺陷 8：v1 用「执行探测」而非「存在性探测」，二进制缺失时异常穿透、绕过「两者皆无」建议分支'
  - ts: '2026-09-16T09:07:29.030Z'
    op: 'claim'
    by: 'omp-architect'
    note: 'branch=feature/OPSP-P22'
  - ts: '2026-09-16T09:09:50.613Z'
    op: 'report'
    by: 'omp-architect'
    note: 'compose 探测缺陷 8 修复完成（自报）：探测统一 try/catch + v1 改存在性探测（command -v）+ docker --version 解析健壮化；「两者皆无」分支现可达并给平台分支建议。'
  - ts: '2026-09-16T09:48:14.076Z'
    op: 'confirm'
    by: '主人'
    note: '未兑现项：对端 v0.9.5 复验（场景①分支化文案、场景②v1 自动回退）待其窗口回报后收口；缺陷 7 已正式收口'
branch: 'feature/OPSP-P22'
report:
  summary: 'compose 探测缺陷 8 修复完成（自报）：探测统一 try/catch + v1 改存在性探测（command -v）+ docker --version 解析健壮化；「两者皆无」分支现可达并给平台分支建议。'
  evidence:
    - '红（对端 v0.9.4 实测）：[EXEC_FAILED] 命令执行失败：docker-compose（异常穿透，未进建议分支）'
    - '绿：bun test read-tier-structure.test.ts → 13 pass（新增 ThrowOnComposeRunner 例：v1 spawn 抛错仍落建议分支，且全程不再直接执行 docker-compose 探测）'
    - 'npm run test: 'ci 全绿（L2 59 pass ×4 文件）'''
    - '旁证：CI 早期无 ps 容器同样得 [EXEC_FAILED] 命令执行失败：ps → 证实「二进制缺失=抛错」，故可选依赖探测必须 try/catch 或存在性探测'
  pending:
    - '真机复验：对端升级 v0.9.5 后复验（v2/v1 皆缺 → 应见「无 CLI 插件机制 + 建议装 v1 单文件」；装上 v1 后应自动回退执行）'
    - '缺陷 7 收口确认（对端已实测确认修复，待其正式回执）'
  at: '2026-09-16T09:09:50.613Z'
  by: 'omp-architect'
confirmedBy: '主人'
confirmedVia: '会话确认 2026-09-16（主人「同意1」）：缺陷 8 修法已入库（存在性探测 + try/catch + 版本解析健壮化）并随 v0.9.5 发布；对端复验排期在其部署交接窗口后（非阻塞）'
