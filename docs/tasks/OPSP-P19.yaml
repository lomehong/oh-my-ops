id: 'OPSP-P19'
title: '策略可见性：omo policy lint / explain（授权 dry-run CLI + /ops-policy 斜杠命令）'
state: '待确认'
level: 'L2'
created: '2026-09-15T14:46:01.997Z'
accept:
  - 'lint：把运行时静默降级显式化——JSON 损坏→全拒、expiresAt 不可解析→规则永久失效、字段错拼（action/service/expires）→被忽略并放宽、production 非布尔→变放行规则、死规则（未知 action / serviceless-serviceBound 错配 / 空数组）、全权与 shell 规则权限等级提示、令牌过期/已消费/永不过期'
  - 'explain：与 ①-a/①-b/③ 同源（policyRequestFor + evaluateAuthorization），输出档位/策略请求/内容硬拒/结论/逐条规则轨迹（failedOn 维度）/逐枚令牌轨迹；只读，不消费令牌'
  - '入口：omo policy lint|explain（启动器子命令，bun 运行 policy-cli.ts，退出码 lint 有错=1 / explain 拒绝=2 / 用法=64）；/ops-policy [lint]|explain 会话内等价并自审计'
  - 'omo status 的策略路径检查修正为 OMO_POLICY_PATH（此前仍查 ~/.ops-pi/policy.json，OMOCONF-1 后恒报未配置）'
  - 'README/启动器 help/OMO_TIPS 同步；bun test / tsc 通过'
events:
  - ts: '2026-09-15T14:46:01.997Z'
    op: 'create'
    by: 'augment-agent-session'
    note: '策略可见性：omo policy lint / explain（授权 dry-run CLI + /ops-policy 斜杠命令）'
  - ts: '2026-09-15T14:46:02.044Z'
    op: 'claim'
    by: 'augment-agent-session'
    note: 'branch=feature/OPSP-P4-orchestration-crossagent'
  - ts: '2026-09-15T14:46:02.091Z'
    op: 'report'
    by: 'augment-agent-session'
    note: 'P19 策略可见性完成：core policy-lint 纯函数 + traceRules/traceTokens；extension policy-explain（explain/lint/格式化）+ policy-cli + /ops-policy；启动器 policy 子命令与 status 路径修正；单测 228 pass（新增 24），tsc 零错'
branch: 'feature/OPSP-P4-orchestration-crossagent'
report:
  summary: 'P19 策略可见性完成：core policy-lint 纯函数 + traceRules/traceTokens；extension policy-explain（explain/lint/格式化）+ policy-cli + /ops-policy；启动器 policy 子命令与 status 路径修正；单测 228 pass（新增 24），tsc 零错'
  evidence:
    - 'core/policy-lint.ts：lintPolicySource/lintPolicyRules/lintTokenSource/summarizeLint；core/policy.ts 导出 ruleActive/traceRule/traceRules + rules 快照；core/tokens.ts 导出 tokenActive/tokenCovers/traceTokens + list()；loadTokenStore 返回 ReloadableTokenStore'
    - 'extension/request.ts POLICY_ACTIONS 词表（serviceless/serviceBound）与 policyRequestFor 同源，测试断言每个显式 action 归类正确'
    - 'extension/policy-explain.ts：parseExplainArgs（引号内空白不切分）、explainAuthorization、formatExplain、runPolicyLint、formatLint；policy-cli.ts runPolicyCli（import.meta.main 守卫，扩展目录内不被宿主加载）'
    - 'commands.ts /ops-policy：lint 缺省；explain 走 ctx（ReloadableTargetPolicy/ReloadableTokenStore 真实路径）；结果按 error/warn 级别 notify；recordAudit 自审计'
    - 'install.sh 启动器：policy) 子命令（bun 缺失时明确提示改用 /ops-policy）；status 策略行改查 $OMO_POLICY_PATH；help/OMO_TIPS 增条目；渲染后 bash -n 通过。bin/omo 同步 policy 子命令'
    - '测试：core policy-lint.test.ts 13 项（含 traceRules 与 allows/isProduction 逐请求一致性）；extension policy-explain.test.ts 11 项（含真实文件 CLI 退出码 0/1/2/64）；对 docs/examples/policy.json.example 实跑 lint/explain 输出符合预期'
    - 'types/vendor-platform.d.ts bun:test 桩补 afterAll/beforeAll/afterEach/beforeEach/toBeLessThan——顺带消除 audit-view.test.ts 4 条既有 tsc 错误'
  pending:
    - '真机（Linux + omo 启动器）验收：omo policy lint / explain 在 ~/.omo 部署下可运行（bun 解析 $EXT/policy-cli.ts 与 node_modules/@ops-pi/core）；omo status 策略行显示真实路径'
  at: '2026-09-15T14:46:02.091Z'
  by: 'augment-agent-session'
