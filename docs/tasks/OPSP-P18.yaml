id: 'OPSP-P18'
title: '授权闭环加固：kb 显式 action + default fail-fast、独立审计存储、路径守卫、shell timeout 修复'
state: '已落定'
level: 'L2'
created: '2026-09-15T14:22:33.114Z'
accept:
  - 'ops_kb_save/ops_kb_sync 携带显式 action（kb-write/kb-sync）；policyRequestFor default 分支对非 read 档 fail-fast；一致性测试覆盖 TIER_TABLE 全部非 read 工具'
  - 'ops_shell_exec/ops_shell_script 的 timeout（z.number）实参生效并受 600s 上限约束'
  - '独立审计：append-only JSONL + sha256 哈希链，落 policy 同级 audit/；与会话解耦（--no-session 下仍持久）；/ops-audit [n] file 可读；/ops-status 校验链完整性'
  - 'PathGuard：机密根（$HOME/.omp、.ssh、vault、token、~/.omo/home）拒读拒写；信任根（policy/config/审计/私有域）拒写；realpath 归一防符号链接；仅本机'
  - 'README/policy.json.example 标注 @local+shell 权限等级与路径守卫语义；bun test / tsc 通过（Windows 环境 3 项既有平台性失败除外）'
events:
  - ts: '2026-09-15T14:22:33.114Z'
    op: 'create'
    by: 'augment-agent-session'
    note: '授权闭环加固：kb 显式 action + default fail-fast、独立审计存储、路径守卫、shell timeout 修复'
  - ts: '2026-09-15T14:22:33.164Z'
    op: 'claim'
    by: 'augment-agent-session'
    note: 'branch=feature/OPSP-P4-orchestration-crossagent'
  - ts: '2026-09-15T14:22:33.215Z'
    op: 'report'
    by: 'augment-agent-session'
    note: 'P18 授权闭环加固完成：kb 显式 action + default fail-fast、shell timeout 修复、独立哈希链审计、PathGuard 机密根/信任根守卫；单测 204 pass（新增 25），tsc 源码零错'
  - ts: '2026-09-15T14:26:36.141Z'
    op: 'confirm'
    by: '主人'
    note: '主人验证通过；pending 项（Linux 真机 --no-session 审计落盘 / 设计文档补录）转入后续跟进'
branch: 'feature/OPSP-P4-orchestration-crossagent'
report:
  summary: 'P18 授权闭环加固完成：kb 显式 action + default fail-fast、shell timeout 修复、独立哈希链审计、PathGuard 机密根/信任根守卫；单测 204 pass（新增 25），tsc 源码零错'
  evidence:
    - 'request.ts：ops_kb_save→kb-write、ops_kb_sync→kb-sync（host 恒 @local）；default 分支 tierOf≠READ 即抛 INTERNAL；request.test.ts 一致性测试遍历 TIER_TABLE + 回归用例（仅 shell 规则不再放行 kb 工具）'
    - 'shell.ts：readTimeoutSec 接受 number/数字串，≤0/NaN 回落缺省，上限 600；tools-shell.test.ts 用记录桩 Runner 断言 timeoutMs=120000/600000/30000'
    - 'core/audit.ts AuditLog：seq/prev/hash 链、跨进程续链、verify 报告断链行号、写失败记 lastError 不抛；audit-sink.ts recordAudit 为唯一双写入口（hooks 2 处 + commands 3 处）；session_start 探测可写性并 notify'
    - 'core/path-guard.ts PathGuard：denyRead/denyReadTree/denyWrite + realize（存在的最深祖先 realpath）；OpsContext 从 policyPath/tokenPath/vault/HOME 推导根；read-only.ts/log.ts/write.ts 接线（ops.host===undefined 时生效）'
    - '测试：path-guard.test.ts 8 项（含符号链接、..穿越、grep -r 覆盖）；tools-shell.test.ts 真实 OpsContext 集成 3 项（file-write 授权下 policy.json 仍不可写）；audit.test.ts 5 项（篡改/删行可发现）'
    - '文档：README 策略节权限等级须知 + 路径守卫 + 审计双写；policy.json.example 增 kb-write/kb-sync 规则与 $comment2b 权限提示；ops_kb_save/sync/file_read/file_write 描述同步'
  pending:
    - '真机（Linux + omp-single）验收：--no-session -p 运行后 ~/.omo/audit/ops-audit.jsonl 有条目且 /ops-status 报链完整；bwrap 沙箱下 audit 目录可写性'
    - '设计文档 v4.3 §7.4 未收录 PathGuard 与独立审计层，待 Owner 确认后补录'
  at: '2026-09-15T14:22:33.214Z'
  by: 'augment-agent-session'
confirmedBy: '主人'
confirmedVia: '会话确认（2026-09-15，主人对 P18 交付明示「验证了没有问题，继续推进」）'
