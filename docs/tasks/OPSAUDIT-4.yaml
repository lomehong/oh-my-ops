id: 'OPSAUDIT-4'
title: '冲突路径演练——/ops-audit 实弹观测被拒/令牌/兜底路径（阶段 3 收口）'
state: '待确认'
level: 'L1'
created: '2026-09-14T06:15:13.736Z'
accept:
  - '演练按主人批准的范围执行，全程证据落 docs/reports/（真实命令与输出）'
  - 'A4 闭环实测：被拒调用条目经 /ops-audit 在当前分支可读且字段可辨（authz=blocked/reasonClass/reason）'
  - '演练记录含与既有探针（05②⑤⑥）的一致性对账 + 未决项登记；结果回填台账 evidence'
events:
  - ts: '2026-09-14T06:15:13.737Z'
    op: 'create'
    by: 'omp-architect'
    note: '冲突路径演练——/ops-audit 实弹观测被拒/令牌/兜底路径（阶段 3 收口）'
  - ts: '2026-09-14T06:19:33.081Z'
    op: 'claim'
    by: 'omp-architect'
    note: 'branch=feature/OPSAUDIT-4-drill'
  - ts: '2026-09-14T06:22:00.000Z'
    op: 'dispatch'
    by: 'omp-architect'
    note: '主人指令「打 tag 触发 CI，真实环境安装最新版测试」：本地已打附注标签 v0.6.0（指向 e387862，含 OPSAUDIT-3 全部实现 + 文档；安装脚本与 main 无漂移，@main 一行安装通道兼容）。推送被阻断：容器内无 GitHub 凭据（credential.helper 指向 gh 但 gh 缺失；GH_TOKEN 空串；api.github.com 401 实测）→ 交接主人：在有凭据的环境执行 git push origin feature/OPSAUDIT-3-impl feature/OPSAUDIT-4-drill v0.6.0，push tag 即触发 release.yml（测试→打包→GitHub Release v0.6.0）。'
  - ts: '2026-09-14T06:30:00.000Z'
    op: 'dispatch'
    by: 'omp-architect'
    note: '容器重启后主人注入 GH_TOKEN（fine-grained，93 字符）+ git safe.directory 修复：推送成功——feature/OPSAUDIT-3-impl、feature/OPSAUDIT-4-drill 两分支 + 标签 v0.6.0 已上 origin；Release workflow 已触发（run 34813880041）。待 CI 绿 → 主人真实环境按 docs/reports/ops-audit-conflict-drill-D1-runbook.md 安装实测 D-1。'
  - ts: '2026-09-14T06:40:00.000Z'
    op: 'hotfix'
    by: 'omp-architect'
    note: '主人在真实环境（logstash-124，老 curl）安装 v0.6.0 失败：bootstrap.sh 用 --retry-all-errors（需 curl ≥ 7.71），老 curl 遇未知选项整条失败 → 三源全灭（主人日志实证；版本解析段无该选项故成功）。热修 c84af5e：能力探测（curl --help all 含该选项才附加），下载/校验/解压语义不变；老 curl 忠实模拟端到端通过（下载+sha256+解压 OK）。已打 tag v0.6.1 触发 CI；v0.6.1 Release 的 install.sh 即修复版。注：Release 正文推荐的 @main 一行通道在 main 合并前仍为旧脚本——主人请用 releases/latest/download/install.sh（短版，取制品）。'
  - ts: '2026-09-14T06:52:07.000Z'
    op: 'requirement-captured'
    by: '主人'
    note: '新需求捕获（logstash-124 实测阻塞引发）：安装器必须「自包含 + 与原生 omp 共存」——① 零手工前置（不再要求预装 oh-my-pi ≥18.1.18，omp 运行时由安装器自备于私有目录）；② 不覆盖/不升级/不劫持系统 omp 命令与包管理命名空间；③ 状态面隔离（现 ~/.omp/agent 的 extensions+AGENTS.md 与原生 omp 共享，双向污染面）；④ 版本契约自我兜底（现「镜像自愈=跟随系统 omp」需重定义）。主人要求先复述确认再立项。另捕获安装器健壮性 bug：版本解析失败时 "latest" 被透传为版本号（vlatest 404），需 semver 校验。归属：新任务（建议 OMOINSTALL-1，走 architect-prd-digest 准入），不在 OPSAUDIT-4 演练范围内；对 D-1 的影响待主人定（现在原生装 omp 继续演练 vs 等自包含安装器）。'
  - ts: '2026-09-14T08:20:00.000Z'
    op: 'dispatch'
    by: 'omp-architect'
    note: 'v0.7.0 发布成功（CI run 34821083551 success；Release assets：install.sh + oh-my-ops-v0.7.0.tar.gz 122MB 含 omp-single + sha256）。main 已合并自包含安装器全链（@main 安装通道自愈，v0.6.x 时代的 @main 旧脚本问题消除）。待主人 logstash-124：releases/latest/download/install.sh 安装 → D-1 S1–S7 实测（结果回传对账收口阶段 3）。'
  - ts: '2026-09-14T08:35:00.000Z'
    op: 'evidence'
    by: 'omp-architect'
    note: 'D-1 实弹完成（logstash-124，v0.7.0 自包含安装器，主人实操）：S1 安装五步全绿（bun 自动装 npmmirror 通道）；S2 exec 被拒 [ERR_PERMISSION] guard-unattended（主人贴回 UI 原文存证）；S3 /ops-audit 回看被拒条目成功；S4 /ops-audit 1 自审计闭环成功；S5/S6 参数防御成功。A4 真实环境闭环。演练报告落 docs/reports/ops-audit-conflict-drill.md。'
  - ts: '2026-09-14T08:58:10.711Z'
    op: 'report'
    by: 'omp-architect'
    note: '冲突路径演练完成（自报）：D-1 实弹于 logstash-124（v0.7.0 自包含安装器，主人实操）——被拒调用留痕 + /ops-audit 回看 + 自审计闭环 + 参数防御，全部通过；演练报告 docs/reports/ops-audit-conflict-drill.md（含 D-2/D-3 降级引用与三层对账）。阶段 3 试点功能面完成。自报 ≠ 完成，待主人确认收口'
branch: 'feature/OPSAUDIT-4-drill'
report:
  summary: '冲突路径演练完成（自报）：D-1 实弹于 logstash-124（v0.7.0 自包含安装器，主人实操）——被拒调用留痕 + /ops-audit 回看 + 自审计闭环 + 参数防御，全部通过；演练报告 docs/reports/ops-audit-conflict-drill.md（含 D-2/D-3 降级引用与三层对账）。阶段 3 试点功能面完成。自报 ≠ 完成，待主人确认收口'
  evidence:
    - 'docs/reports/ops-audit-conflict-drill.md：D-1 S1–S7 结果表 + 与 05②⑤⑥/12 探针对账 + 未决三项'
  pending:
    - '主人 confirm OPSAUDIT-4 收口（阶段 3 完结）；suite-map 补录与安装器经验回灌（知识任务，待主人点头）'
  at: '2026-09-14T08:58:10.577Z'
  by: 'omp-architect'
