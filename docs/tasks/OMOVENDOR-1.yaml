id: 'OMOVENDOR-1'
title: 'adapter vendor 完整性防线：回信回归探针入 test:ci + 安装/发布校验 + 溯源清单（消除整文件同步静默回退）'
state: '待确认'
level: 'L1'
created: '2026-09-26T07:50:12.795Z'
accept:
  - '回归防线：scripts/probe-yuyi-reply-frame.mjs 纳入 test: 'ci（与 pre-push、CI 同源），新增直跑入口（建议 npm run test:vendor）；全绿 exit 0、连跑 2 次稳定（约 31-40s，成本在证据中注明）'''
  - '负例自证：将 vendor 副本回信目标临时改回会话别名（模拟上游整文件同步回退）时探针必须变红，实测输出留档（对齐 OMOBRIDGE-2 红基线 3 通过 / 11 失败）'
  - '安装防线：install.sh 对适配器落盘做完整性校验（与仓库内基准比对），不符即中止并明确报错；scripts/probe-install-ops-core.sh 增补「适配器落点存在且一致」断言及篡改负例（现状 71-89 行只查 skills 与 redact，不查适配器 JS）'
  - '溯源清单：vendor/ 新增适配器溯源件（来源 /dist/omp.js 0.1.0 与基准 md5 393e21ba…、本仓补丁 D1-D5 逐条、当前文件 sha256、同步规程「diff 审查 → 探针绿 → 更新清单」）；清单更新为 vendor 变更合并的前置条件'
  - '发布门：release.yml 打包清单断言补 vendor/yuyi-omp-extension.js，打包前执行 vendor 回归探针（失败即停止发布）；发布包抽验含该文件且 sha256 与清单一致'
  - '上游未修残留登记：上游 0.1.0 与对端同源缺陷（附 Hub 评论引用）在溯源件与执行记录显式登记；若完成上游化则以与上游 md5 一致性验证收口（参考 OPSP-P10 先例）'
events:
  - ts: '2026-09-26T07:50:12.796Z'
    op: 'create'
    by: 'qoder-architect(qoder-cli)'
    note: 'adapter vendor 完整性防线：回信回归探针入 test:ci + 安装/发布校验 + 溯源清单（消除整文件同步静默回退）'
  - ts: '2026-09-26T10:41:30.491Z'
    op: 'claim'
    by: 'qoder-architect(qoder-cli)'
    note: 'branch=feature/OMOVENDOR-1'
  - ts: '2026-09-26T11:12:38.604Z'
    op: 'report'
    by: 'qoder-architect(qoder-cli)'
    note: 'OMOVENDOR-1 落地（自报）：① 回归探针入 test:ci + 直跑入口 npm run test:vendor（连跑 2 次 15/15 exit 0，32s/33s）；② 负例自证自动化（D1 回滚 → 9 通过 / 6 失败，红在 T2/T4/T6/T9/T10 且桩 Hub 复现生产原文）；③ 安装前完整性闸门（install.sh 落盘前校验、清单缺失即拒；probe-install 新增 [4c] 7 断言含两条负例）；④ 溯源件 PROVENANCE.md + 清单 .sha256（上游基准 md5 / 当前 sha256 / D1-D5 逐条 / 同步规程）；⑤ 发布门三处（打包前探针、打包清单断言、包内指纹抽验，本地模拟含篡改负例）。顺带修掉探针 Windows 默认路径缺陷（URL.pathname → fileURLToPath）。'
branch: 'feature/OMOVENDOR-1'
report:
  summary: 'OMOVENDOR-1 落地（自报）：① 回归探针入 test:ci + 直跑入口 npm run test:vendor（连跑 2 次 15/15 exit 0，32s/33s）；② 负例自证自动化（D1 回滚 → 9 通过 / 6 失败，红在 T2/T4/T6/T9/T10 且桩 Hub 复现生产原文）；③ 安装前完整性闸门（install.sh 落盘前校验、清单缺失即拒；probe-install 新增 [4c] 7 断言含两条负例）；④ 溯源件 PROVENANCE.md + 清单 .sha256（上游基准 md5 / 当前 sha256 / D1-D5 逐条 / 同步规程）；⑤ 发布门三处（打包前探针、打包清单断言、包内指纹抽验，本地模拟含篡改负例）。顺带修掉探针 Windows 默认路径缺陷（URL.pathname → fileURLToPath）。'
  evidence:
    - 'A1 入口：package.json:15,30-31；红基线=Missing script + 直呼脚本 ERR_MODULE_NOT_FOUND(file:///E:/E:/…)，修于 probe-yuyi-reply-frame.mjs:23,28'
    - 'A2 负例实测：变异命中 2 处（L1562 finalizeTurn / L1620 sendFailureReply）→ 结果：9 通过 / 6 失败 exit=1，桩 Hub 拒收原文「回信去向与原请求发送方不符：目标 "PEER-DEV-omp" 归属 agent (未解析)」；同一负例由 probe-vendor-integrity.sh §3 自动化（变异未命中即判红，防假绿）'
    - 'A3 安装闸门：install.sh:186-203（落盘前校验 + 清单缺失拒装）；probe-install-ops-core.sh:94-126 [4c] 全绿 7 项，失效前红灯为「适配器指纹不符竟安装成功（闸门失效）」'
    - 'A4 溯源：vendor/yuyi-omp-extension.PROVENANCE.md（上游 /dist/omp.js 0.1.0 md5 393e21ba…、当前 sha256 ec25ec8e…/md5 5f4df589…、D1-D5 逐条含行号、同步规程五步、上游未修登记）+ vendor/yuyi-omp-extension.sha256；清单/溯源一致性进体检 §1/§2（漂移即红）'
    - 'A5 发布门：release.yml:65-67 打包前探针（直呼脚本不随 package.json 漂移）、:90 打包清单断言（适配器+清单+溯源件）、:110-113 包内指纹抽验；本地逐字模拟抽验「清单==包内件」且篡改包被识别；npm run test:workflow 全绿'
    - 'A6 上游残留登记：溯源件 §四（Hub 评论 id 2026-09-16T00-05-14-5d5dx2 + URL + 对端已知会 + 「md5 一致 + 探针绿」收口方式）'
    - '环境限定红项（预存在，已取证）：probe-install [6] 0600（Git Bash chmod 600→644，HEAD 版安装器同一探针同样红）；L1 ssh.test.ts 2 项（git worktree 纯净树复跑同样 8 pass/2 fail）；L2 14 项（测试文件对本次改动件零引用）'
    - '执行记录：docs/designs/omovendor-1-执行记录.md（含测试清单先行、Do→行号映射、红/绿实测输出、环境限定红项登记、可回源性）'
  pending:
    - '发布门三处仅在下一个 v* tag 真跑（本地已逐字模拟 + 结构守卫）；安装闸门未在 Linux 真机实证（本机 0600 类断言不可证）'
    - '上游未修（D1/D2/D3）：收口需上游修复并分发后以「上游件 md5 一致 + 本仓探针绿」双证撤线'
  at: '2026-09-26T11:12:38.602Z'
  by: 'qoder-architect(qoder-cli)'
