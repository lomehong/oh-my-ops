id: 'OPSP-P15'
title: 'Runbook 知识库 + GitHub git 共享同步'
state: '待确认'
level: 'L2'
created: '2026-09-14T12:57:29.078Z'
accept:
  - '本地知识库 ~/.omo/knowledge/（markdown）+ ops_kb_list/ops_kb_search（read 档）+ ops_kb_save（write 档）'
  - 'git 同步：init/commit/push/pull --rebase 全 best-effort，失败不阻塞本地读写（离线可用）'
  - '无远端 = 本地模式优雅降级；Owner 建仓填 remote 后完整生效'
  - '运行时验收：save → search 命中 → sync 推送真源 → 第二节点 pull 可见'
events:
  - ts: '2026-09-14T12:57:29.085Z'
    op: 'create'
    by: 'omp-architect-session'
    note: 'Runbook 知识库 + GitHub git 共享同步'
  - ts: '2026-09-14T12:57:44.407Z'
    op: 'claim'
    by: 'omp-architect-session'
    note: 'branch=feature/OPSP-P15-knowledge'
  - ts: '2026-09-14T13:16:31.013Z'
    op: 'report'
    by: 'omp-architect-session'
    note: 'P15 Runbook 知识库 + git 共享——实现完成，单测 5/5 + 本地 e2e 全链路通过（save→search→sync）'
branch: 'feature/OPSP-P15-knowledge'
report:
  summary: 'P15 Runbook 知识库 + git 共享——实现完成，单测 5/5 + 本地 e2e 全链路通过（save→search→sync）'
  evidence:
    - 'KnowledgeStore：markdown+front-matter，slug 穿越防护，覆盖保留 createdAt，大小写不敏感检索'
    - '工具：kb_list/kb_search（read 档）+ kb_save/kb_sync（write 档）；TIER_TABLE 登记，提示词能力清单自动同步'
    - 'gitSync：init/commit/push/pull --rebase autostash 全 best-effort；identity 兜底；无远端=本地模式优雅降级'
    - '两节点共享真源单测（file:// 裸远端往返）：A push → B pull 可见 ✓'
    - 'e2e（重装后真实 omo）：kb_save ✓ → kb_search 命中 ✓ → kb_sync commit ✓；条目落盘 ~/.omo/knowledge/'
    - '（已闭环）Owner 建仓 lomehong/omo-knowledge（private）+ config.repo 接线；ops_kb_sync 推送 commit 646ec80 成功'
    - '（已闭环）第二节点 clone 真源可见 ssh-502-runbook.md（多节点共享验证通过）'
    - '目标机 push 凭据：目标机 shell 需 GH_TOKEN 或 credential store（ Pull 对私有仓亦需认证）；缺失时本地保留重试，不阻塞'
  at: '2026-09-14T13:16:31.013Z'
  by: 'omp-architect-session'
