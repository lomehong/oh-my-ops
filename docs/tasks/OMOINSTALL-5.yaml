id: 'OMOINSTALL-5'
title: 'bootstrap 版本解析过窄：仅认 302 重定向，代理/镜像 200 直返页面时三源全落 latest 致安装被拒'
state: '已落定'
level: 'L1'
created: '2026-09-16T06:13:47.465Z'
accept:
  - '解析策略扩展为三条：① 资产 releases/latest/download/install.sh 的 Location 头 ② 页面内 /releases/tag/vX.Y.Z ③ url_effective basename（原策略）'
  - '末位回退 GitHub API releases/latest 的 tag_name'
  - '新增 --resolve-only（仅解析打印版本，不下载不安装；其余参数透传 install.sh）'
  - '回归守卫 scripts/probe-bootstrap-version.sh：本地假代理复现「200 无重定向」，旧逻辑红（复刻对端原文）、新逻辑绿；纳入 test: 'ci'''
  - '发布 v0.9.2 后通知对端：其 bootstrap 一行命令可直接安装（无需 OMO_VERSION 手工指定）'
events:
  - ts: '2026-09-16T06:13:47.466Z'
    op: 'create'
    by: 'omp-architect'
    note: 'bootstrap 版本解析过窄：仅认 302 重定向，代理/镜像 200 直返页面时三源全落 latest 致安装被拒'
  - ts: '2026-09-16T06:13:47.606Z'
    op: 'claim'
    by: 'omp-architect'
    note: 'branch=feature/OMOINSTALL-5'
  - ts: '2026-09-16T06:17:22.671Z'
    op: 'report'
    by: 'omp-architect'
    note: 'bootstrap 版本解析修复完成（自报）：解析扩展为三策略（资产 Location → 页面 tag 链接 → url_effective）+ GitHub API 末位回退；新增 --resolve-only；新增纯离线假代理回归守卫并纳入 test:ci。已合并 main（e6ff6df）。'
  - ts: '2026-09-16T09:48:13.924Z'
    op: 'confirm'
    by: '主人'
    note: '未兑现项：对端一行安装已实证可用；v0.9.3+ 的 CDN 传播亦已实测'
branch: 'feature/OMOINSTALL-5'
report:
  summary: 'bootstrap 版本解析修复完成（自报）：解析扩展为三策略（资产 Location → 页面 tag 链接 → url_effective）+ GitHub API 末位回退；新增 --resolve-only；新增纯离线假代理回归守卫并纳入 test:ci。已合并 main（e6ff6df）。'
  evidence:
    - '红复现：旧解析段（git show HEAD 版）在「200 直返、无重定向」假代理下输出与对端一字不差（latest×3 + 拒绝下载）'
    - '绿：bash scripts/probe-bootstrap-version.sh → 3/3 全绿（0.6s，纯离线；假代理 HTTP/1.0 + 线程化 + pid 落盘）'
    - '真机三路径实测：url_effective → /releases/tag/v0.9.1；资产 Location → /download/v0.9.1/install.sh；API tag_name → v0.9.1'
    - 'npm run test: 'ci（含 test:install + test:bootstrap）全绿；bash -n scripts/bootstrap.sh PASS'''
    - '推送：38b04b1..e6ff6df main -> main（pre-push 自动跑 test: 'ci 全绿）'''
  pending:
    - 'v0.9.2 发布（需主人授权打 tag）——发布后对端 README 一行命令可直接安装'
    - '对端验收（一行安装成功 + omo status 正常）'
    - '跨任务未修：启动时 7 条 Custom tool load failed 噪音（见 OMOINSTALL-4）'
  at: '2026-09-16T06:17:22.670Z'
  by: 'omp-architect'
confirmedBy: '主人'
confirmedVia: '会话确认 2026-09-16（主人「同意1」）：对端 README 一行命令（bootstrap）现可直接安装；解析三策略 + API 回退已发布（v0.9.2），守卫 3/3 并入 test:ci'
