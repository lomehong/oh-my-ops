id: 'CIPORT-1'
title: 'test:ci 跨平台可推：Windows 红项归零（L1 ssh ×2 / L2 平台性 / 安装器 0600 探针）+ 钩子与 CI 口径对齐'
state: '待确认'
level: 'L1'
created: '2026-09-26T11:53:51.986Z'
accept:
  - '全量枚举：test: 'ci 全部步骤在本机（Windows/Git Bash）逐项独立执行，红项清单与耗时落档（不因失败中断）'''
  - '逐项归因 + 修复：每个红项给出根因（平台语义差异或真实缺陷）与红→绿证据；修复不得弱化 POSIX 平台的原判据'
  - '平台能力探测统一化：跳过类修复必须运行时探测（不得硬编码平台名）且显式打印 SKIP 原因；支持平台上原断言照跑'
  - 'npm run test: 'ci 本机连续 2 次全绿（含 test:install / kb 链 / vendor 双防线），exit=0'''
  - '打 tag 触发 CI 路径不受影响：release.yml 触发与打包闸门保持；钩子/README/工作流注释与 test: 'ci 实际链条口径对齐（消除漂移）'''
  - '修复范围最小化：优先测试/探针侧平台自适应；若红项暴露真实缺陷则单独列出并附最小修复与影响面说明'
events:
  - ts: '2026-09-26T11:53:52.006Z'
    op: 'create'
    by: 'qoder-architect(qoder-cli)'
    note: 'test:ci 跨平台可推：Windows 红项归零（L1 ssh ×2 / L2 平台性 / 安装器 0600 探针）+ 钩子与 CI 口径对齐'
  - ts: '2026-09-26T11:53:52.212Z'
    op: 'claim'
    by: 'qoder-architect(qoder-cli)'
    note: 'branch=feature/CIPORT-1'
  - ts: '2026-09-26T14:35:39.000Z'
    op: 'report'
    by: 'qoder-architect(qoder-cli)'
    note: 'CIPORT-1 落地（自报）：Windows/Git Bash 下 test:ci 13 步全绿可推——① 枚举先行（13 步逐项跑，红 8/绿 5、≈788s）；② 平台语义类修复 8 项（L1 ssh 路径断言 / L2 预算 60s / 探针 0600 能力分级 ×4 / bootstrap 假代理换 bun / EXIT-trap+set -e 缺陷与进程库 / MSYS 路径归一 / 口径漂移）；③ 真实缺陷 7 项 + 1 待决（webhook resp.ok）；④ 连续两次严格对 #4/#5 起点=终点=74f3b320…，exit=0、✗=0；⑤ 判据未弱化：强制置位负例红（644/666 双抛）+ 污染护栏变异负例（13/14 + 复现）；⑥ tag→CI 路径原样（release.yml 触发面 + 三处打包闸门，test:workflow 绿）。'
branch: 'feature/CIPORT-1'
report:
  summary: 'CIPORT-1 落地（自报）：Windows/Git Bash 下 test:ci 13 步全绿、可推。① 枚举先行：13 步逐项独立执行，红 8 步 / 绿 5 步、≈788s 落档（/e/tmp/ciport-enum/）；② 平台语义类修复 8 项：L1 ssh 受管路径断言、L2 预算 60s（11 文件）、探针 0600 能力分级 ×4、bootstrap 假代理换 bun、探针 EXIT-trap + set -e 缺陷（新 proc.sh 双命名空间进程库）、MSYS 路径归一、口径漂移四处；③ 真实缺陷 7 项：kb-sync 凭据 helper 链重置（安全面）、hostname -I GNU-only 静默失败、config.env MSYS 路径、接班进程静默消失、0600 硬拦锁死、--outfile /dev/null 落 nul、selftest 负例默认路径污染仓库根（run #3 指纹漂移根因）；另登记 1 待决（webhook 写回未查 resp.ok）；④ 连续两次全绿严格对：#4 22:06:20–22:20:23、#5 22:20:33–22:34:39，起点=终点指纹 74f3b320…，两次 TEST_CI_EXIT=0、✗=0；⑤ 判据未弱化自证：0600 探测强制置位负例（bash 644 / mjs 666 双双抛出）+ 污染护栏变异负例（撤销修复 → 13/14 且污染复现）；⑥ 打 tag 触发 CI 路径保持：release.yml 触发与打包三处闸门原样，test:workflow 绿。'
  evidence:
    - '枚举红基线：/e/tmp/ciport-enum/（13 份 log + SUMMARY；红 8 步：l1、l2、enroll-install、kb-ui、kb-lint、install、bootstrap、typecheck[依赖未装齐，装齐自愈]）'
    - '全链绿：#1（无标记）、#2 21:32:48–21:46:38、#3 21:48:14–22:02:17、#4 22:06:20–22:20:23、#5 22:20:33–22:34:39（TEST_CI_EXIT=0，✗=0；l1 140/0、l2 135/0、自检 14/14、结果：全绿 ×5、0 项失败 ×2）'
    - '冻结树指纹（不含执行记录文档）：修前 23b966ac…（#3 终点 c114434d…＝selftest 落盘污染所致）；修后 74f3b320…（#4 起点=#4 终点=#5 终点，严格对判据）'
    - '判据未弱化自证：secret-perm.sh 强制 _OMO_PERM_CACHE=yes →「✗ 秘密文件权限过宽（应 0600）：… 当前 644 —— 修复：chmod 600 …」exit=1；secret-perm.mjs 变异副本强制 true →「… 当前 666」抛出；降级仅在本机实测不可表达 0600 时发生且必打印 ⚠（绝不静默）'
    - '污染缺陷（执行记录 §六-8）：kb:provision:selftest 负例缺 --registry/--deliver ⇒ 仓库根落 n-credential.json（伪秘密）+ ops-kb-registry.json（run #2 21:34:15、run #3 21:49:41、单步复现 22:04 三处实证）；修复 ops-kb-provision.mjs:325-326 + 新增「不污染 CWD」断言 :333-337；变异负例 13/14 且污染复现，修复版 14/14 无散落'
    - '探针收尾：#1–#5 各跑后核查无残留 bun 服务进程（kill -0 / tasklist 双命名空间）、无 nul、无临时目录句柄泄漏（scripts/lib/proc.sh + 各探针 cleanup set +e 首行）'
    - 'release 触发面：.github/workflows/release.yml:3-5（on: push: tags ["v*"]）原样；三处闸门原样（包前 vendor 探针 :66-67、打包清单断言 :90-104、包内指纹抽验 :112-115）；secret-perm.sh/.mjs + proc.sh 入清单与打包（:97-98,127-130）；npm run test:workflow 绿'
    - '执行记录：docs/designs/ciport-1-执行记录.md（测试清单先行 A1–A6、枚举基线、Do→文件/行映射、红→绿实测引文、严格对与指纹定义、强制置位负例、真实缺陷登记、未兑现/待真机、可回源性）'
  pending:
    - 'CI（release.yml）本身尚未真跑过：本地为结构守卫 + 逐字模拟；打 tag 后首次真跑（结果并入 confirm 前留档）'
    - 'POSIX 严格分支与 test:install / enroll-install 的 Linux 语义未在 Linux 真机复跑（本机以强制置位负例自证）'
    - 'test:web-verify 本机 SKIP（无 Chrome）；test:l2 的 60000ms 预算未在慢盘/冷缓存机器验证'
    - 'webhook 写回未检查 resp.ok（真实缺陷 #7）登记待决，不在本次范围'
  at: '2026-09-26T14:35:39.000Z'
  by: 'qoder-architect(qoder-cli)'
