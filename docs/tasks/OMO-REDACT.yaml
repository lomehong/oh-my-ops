id: 'OMO-REDACT'
title: 'omo 脱敏移植：模型厂商边界双向脱敏（出站掩码 + 入站还原）'
state: '待确认'
level: 'L1'
created: '2026-09-18T06:00:00.000Z'
accept:
  - '引擎移植到 ops-core（纯逻辑）：五类内置规则（含身份证校验位、Luhn）、自定义正则、别名、占位符记账、按会话账本、原子 0600 落盘、配置装载'
  - '装配到 ops-extension：before_provider_request 出站掩码（返回替换 payload，no-op 时零干预）；tool_call 入站还原（best-effort）'
  - '★ 工具层还原（omo 原生保证）：registerOpsTool 单一收口，执行前把参数里的占位符还原为真实值——不依赖宿主事件层语义'
  - '账本落在 $OMO_DIR/home/.omp/redact（PathGuard 机密根内，Agent 读不到）+ 配置示例 + README 与已知边界'
  - '守卫：19 例（含密钥/Bearer/赋值式/PEM/JWT、身份证校验位、Luhn、重叠优先级、别名可逆、深遍历惰性克隆、账本 TTL/上限/重启续号、状态原子写与损坏兼容、配置缺省与告警、装配端到端与未启用零干预、PathGuard 拒读）'
events:
  - ts: '2026-09-18T06:00:00.000Z'
    op: 'create'
    by: 'omp-architect'
    note: '主人要求把 omp-redact-extension.js（脱敏插件）移植进 omo'
  - ts: '2026-09-18T06:40:00.000Z'
    op: 'report'
    by: 'omp-architect'
    note: '移植落地（自报）：引擎入 ops-core/src/redact.ts（保留语义 1:1）；装配入 ops-extension/src/redact.ts（出站 before_provider_request + 入站 tool_call）；**并在 registerOpsTool 加工具层还原**——实测宿主 tool_call 结果契约的 input 是「原始执行入参」，参考插件还原的是归一化视图 ⇒ 还原被忽略（真机复现：13889332920 原样到达 bash），故在 omo 自己的工具入口兜底保证生效。账本落私有 HOME/.omp/redact（PathGuard 机密根）；测试 19 例全绿；README 与配置示例就位。附带教训：写测试时若断言里含占位符/秘密字面量，会被本机启用的脱敏插件改写 ⇒ 固定值与期望值一律运行时计算。'
