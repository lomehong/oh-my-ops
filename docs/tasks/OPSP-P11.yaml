id: 'OPSP-P11'
title: 'write 档 policy 维度补齐：ops_file_write/ops_vault_store 显式 action 映射 + ruleCovers 收紧（安全修复）'
state: '待确认'
level: 'L1'
created: '2026-09-14T01:52:25.476Z'
accept:
  - 'ops_file_write→action=''file-write''、ops_vault_store→action=''vault-write'' 经 policyRequestFor 统一映射，三层（①-a/①-b/③）同值'
  - 'ruleCovers 收紧：请求缺 action 维度一律不覆盖（write 档不再被 services/shell 类规则连带放行），回归测试覆盖'
  - 'README + policy.json.example 同步说明 write 档 action；既有测试全绿'
events:
  - ts: '2026-09-14T01:52:25.479Z'
    op: 'create'
    by: 'omo-impl-session'
    note: 'write 档 policy 维度补齐：ops_file_write/ops_vault_store 显式 action 映射 + ruleCovers 收紧（安全修复）'
  - ts: '2026-09-14T01:52:34.999Z'
    op: 'claim'
    by: 'omo-impl-session'
    note: 'branch=main'
  - ts: '2026-09-14T01:59:19.098Z'
    op: 'report'
    by: 'omo-impl-session'
    note: 'P11 write 档 policy 维度补齐——实现完成，安全缺口闭合（三层同值 + 回归覆盖）'
branch: 'main'
report:
  summary: 'P11 write 档 policy 维度补齐——实现完成，安全缺口闭合（三层同值 + 回归覆盖）'
  evidence:
    - 'request.ts：ops_file_write→action=''file-write''、ops_vault_store→action=''vault-write'' 显式映射（①-a/①-b/③ 三层经 policyRequestFor 同值）'
    - 'policy.ts ruleCovers 收紧：规则带 services 白名单而请求无 service 维度 → 不覆盖（服务级规则不再连带放行整机授权请求）'
    - '修复的缺口：此前 ops_file_write/ops_vault_store 落 default 分支仅 {host}——任何命中 host 的规则（哪怕只授权 nginx/restart 或 shell）都会放行任意文件写入与 vault 写入'
    - '测试：core 新增 3 用例（服务级不连带/显式 file-write 规则/全 host 规则语义保持）；guards.test 新增 ③ 复核正负用例；request.test 新增映射用例'
    - '验证：tsc 双包 0 错；core node --test 91 用例 80 过 3 失败（均为 Windows 环境噪音：sh/0600，与基线一致）；L2 bun 61/61'
    - '文档：README policy.json 段 + policy.json.example 同步（host 远程语义、file-write/vault-write 显式授权）'
  pending:
    - '需 Owner 确认落定；已有部署若依赖旧行为（shell 规则连带放行写档）需在 policy.json 显式补 file-write/vault-write'
  at: '2026-09-14T01:59:19.095Z'
  by: 'omo-impl-session'
